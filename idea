Запуск программы(в cmd):
yanmisis\Scripts\activate

cd Desktop\python_labs\src\lab01

python "название файла".py





в терминале(visual studio code)
git add .    <-- создание гита
git commit -m "название" 
git push     <-- сохранение в репозиторий на сайте


list.sort() - сортирует на месте 
sorted(list) - создает новыйотсортированный список
min, max - быстрый поиск
in, index - проверка поиска

cd папка - добавляет путь 
.venv\Scripts\activateю.bat - запускает среду



elements = list(freq.items()) # меняет что-то (словарь) на список 
elements.sort(key=lambda x: x[0]) #
'''
sort - сортировка по алфавиту от A к Z; x[0] выполняет сортировку от начала 0 индекса
key - аргумент, принимающий функцию. Эта функция применяется к каждому элементу
списка для определения порядка сортировки.
lambda x: ... - это анонимная (лямбда) функция, которая возвращает значение,
по которому и происходит сортировка
'''
return [:n] # список[start:stop]